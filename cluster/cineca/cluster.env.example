# Copy to cluster.env and fill values discovered by scripts/probe_cluster.sh.
# Never put a password, OTP, recovery code, or private key in this file.
CINECA_USER=bliu0001
CINECA_EMAIL=boliang.liu@campus.tu-berlin.de
CINECA_HOST=login.leonardo.cineca.it

# Populated only after a successful SSH probe.
CINECA_ACCOUNT=AIFAC_S07_051
CINECA_GPU_PARTITION=boost_usr_prod
CINECA_GPU_QOS=boost_qos_dbg

# Safe smoke-test defaults. Production LuLu jobs need a separate resource review.
CINECA_SMOKE_GPUS=1
CINECA_SMOKE_TIME=00:05:00
