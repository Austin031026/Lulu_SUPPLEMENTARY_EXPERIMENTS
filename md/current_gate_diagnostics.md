# Current bounded ReN diagnostics (2026-09-17)

This experiment follows the newly authorized three-arm comparison, including Vanilla OPD. It does not train a new gate.

Output: `LuLu_outputs/experiments/ren_gate_diagnostics_8k_r4_s42_20260917`.

1. Reuse 48 gold/wrong cached rollouts (10,433 sampled reasoning positions), auditing `g=max(E_q[log pH−log pC],0)` and `w=g/(1+g)`. Current FP32 weight caches are sufficient for the first analysis. Recompute signed gaps from the existing C/H/W/T hidden states using both BF16 and FP32 heads; no new backbone passes or continuation generation for this part. Check reconstructed gold weights against both prior precision branches.
2. Reuse the 216 intervention states / 864 continuations. Define high-w from the weighted90th percentile of gold cached positions, before reading utilities. This is only an audit grouping. Report raw balance and effects, and original-triplet fixed-effect regressions controlling Teacher KL, entropy, absolute position, TV, max/actual token probability. Bootstrap original-rollout clusters within snapshot; no token-level independence or noisy state-label AUC. Original selection was by lambda, so conclusions remain conditional on that selected set. Compute `s=max(DW−DH,0)` only for auditing, also reporting the signed/reverse counterpart because rectification alone creates positive mean under symmetric noise. One wrong donor cannot establish donor robustness.
3. Gradient cosines: actual pre-update parameters3/7/11 are unavailable. Use saved16k checkpoints after0/4/8/12 updates on the same16 hash-selected Base rollouts (133,433 generated tokens), with all tokens and full parameters. Recompute H/C at each checkpoint; Teacher and fixed-reference forward states are shared. Four Student workers, one reference worker, and TeacherTP2 occupy seven idle GPUs selected by the controller. This is a fixed-batch checkpoint comparison, not an exact reconstruction of historical on-policy gradients.
4. Three fresh Base trainings, all full parameter, same first4 rounds of64 prompts from pool2048,8k response horizon, one on-policy refresh/update per round. Four StudentDDP/residentvLLM workers, one H/reference, and TeacherTP2 on seven idle GPUs. Keep four Student workers for all three arms; physical IDs are selected at launch and recorded in launches/*.json. Run arms sequentially to keep the existing tested services. A uses reasoning weight0; B uses1; C usesboundedReN. All use reasoning-token mean within rollout then prompt mean, unchanged global-generated-token control/reference and coefficients, LR1e-6. Use `--method ren_balanced --reasoning-ablation none|vanilla|ren`; the old `vanilla_opd` backend does not supply the same control/reference and is not the intended comparison.
5. Record exact norm+pairwise cosine at updates1/4. Rank0 archives at most two gradient vectors on CPU (~13.8GB for1.7B FP32), with bounded GPU dot-product buffers. No extra model copies, parameter updates, or additional backward passes for cosine beyond the existing three component passes. Zero-norm cosine is undefined (`null`). Raw rho diagnostics remain counterfactualReN for A/B; applied weight and arm are explicitly logged.
6. Freeze disjoint DAPO dev128 by SHA ordering before training, sampled0.6/0.95/20 at32768 horizon. Evaluate onlyRound4 of A/B/C. Also repeat the fixed five external cohorts (≤199 per set) to diagnose cross-task transfer. Generate Base dev128 once; reuse already rescored32k Base external predictions. Never truncate prompts; retain128 context safety tokens. No checkpoint selection based on tests.

Run: `python Lulu/scripts/run_gate_diagnostics.py --run --detach` using the existing TRL environment. The controller snapshots sources and input hashes and uses one GPU idle across4 checks for the head-only audit, then admits the gradient audit on seven idle GPUs (four checks), uses seven idle GPUs for training, and uses all currently idle GPUs (at least one, up to eight) for evaluation. A busy unrelated GPU never blocks evaluation or a seven-GPU training allocation; evaluator shard counts are read from each actual eval_plan.json. Other GPU jobs are not stopped. `live_progress.json` gives the current stage; final outputs are `REPORT.md` and `comparison.json`.

Tests:20 CPU tests passed, including explicit dense losses/gradients for all3 arms, unchanged optimizer state during diagnostics, exact pairwise cosine, zero-norm handling, and uneven-DDP reduction. In-memory Student scoring caches are regenerated as usual each on-policy round. Failed/capped trajectories are retained, and no weight-sum or mean renormalization is applied.

Recovery (2026-09-17): only explicit NCCL system/transport errors may trigger automatic training retries, at most two per arm, with the attempt count persisted across controller restarts. Each attempt records its command, physical GPU assignment, log byte offset and checkpoint in `launches/*.attempt-*.json`. Completed arms are skipped. Recovery reads the atomic latest checkpoint and restores weights plus optimizer; uncommitted rollout/diagnostic files move to `recovery/` before fresh on-policy collection. Physical GPU IDs may change while role counts and scientific options remain fixed. Missing optimizer, unfinished committed-round reporting, numerical/data errors and OOM stop for inspection. Child process groups are cleaned up before resources are acquired again. NCCL INFO logs are enabled to diagnose recurrence; the precise cause of the first `pthread_join` error remains unconfirmed.

Second recovery (12:07 UTC): A/B both completed four rounds. C failed before update1 when the Teacher NCCL watchdog stopped making progress for480 seconds. The retry classifier now includes this explicit watchdog failure and collective timeouts, still excluding OOM/data/numerical errors and keeping the original two-retry budget. Teacher CPU instructions and lifecycle barriers now use an independent Gloo group; model reductions remain NCCL TP2. Initialization uses the ordinary NCCL communicator with a completed device barrier/synchronization before model load, instead of bound-device nonblocking init. This mitigates observed transport risks; it is not proof of the sole root cause. CPU tests and both native/eager-local tiny two-GPU parity tests pass. No objective, model, precision, batch, data or sampling changes.
